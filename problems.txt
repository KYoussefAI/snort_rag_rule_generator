# Remaining Problems / Manual Evidence Needed

This file lists only the items that could not be completed in this environment or still need final manual execution based on `most_important_file.pdf` and `snort_rag_codex_final_fix_handoff.pdf`.

## 1. Real LLM Benchmarking Not Executed

Status: still required for final evidence.

The controlled RAG + LLM pipeline works and was smoke-tested with the `mock` client. However, the assignment requires benchmarking real LLMs. The current `results/llm_benchmark.csv` and `results/llm_benchmark_summary.csv` are mock-only evidence.

Required local commands:

```bash
ollama serve
ollama pull mistral
ollama pull llama3
ollama pull qwen2.5
PYTHONPATH=src python scripts/benchmark_llms.py --models ollama:mistral ollama:llama3 ollama:qwen2.5
```

Success condition: `results/llm_benchmark_summary.csv` contains real model rows, not only `model_spec=mock`.

## 2. Real Snort Runtime Validation Not Executed

Status: blocked by missing Snort binary in this environment.

`results/snort_runtime_validation.csv` was regenerated with richer metadata, but all rows are `SKIPPED` because `snort` was not found. All 180 exported rules passed local structural validation, but that is not the same as real Snort runtime validation.

Required local command:

```bash
PYTHONPATH=src python scripts/run_snort_validation.py --snort-bin snort --rules data/processed/person1_rules.rules --out results/snort_runtime_validation.csv
```

If Snort requires a config:

```bash
PYTHONPATH=src python scripts/run_snort_validation.py --snort-bin snort --config "$SNORT_CONFIG" --rules data/processed/person1_rules.rules --out results/snort_runtime_validation.csv
```

Success condition: CSV rows become real `PASS` or `FAIL`, not `SKIPPED`.

## 3. PCAP Replay / Empirical False-Positive Evidence Not Executed

Status: blocked by missing Snort binary in this environment.

Synthetic lab PCAPs were generated in `tests/pcaps/generated/`, but `results/pcap_test_results.csv` is all `SKIPPED` because Snort replay could not run. This means empirical benign false-positive evidence is still missing.

Required local commands:

```bash
PYTHONPATH=src python scripts/generate_lab_pcaps.py --out tests/pcaps/generated
PYTHONPATH=src python scripts/run_pcap_tests.py --snort-bin snort --rules data/processed/person1_rules.rules --pcap-dir tests/pcaps/generated --out results/pcap_test_results.csv
```

Success condition: malicious PCAP rows contain alert counts and triggered SIDs, and `benign_traffic.pcap` records its real alert count.

## 4. Final Binary Report Must Be Regenerated After Manual Evidence

Status: still required before final submission.

Markdown report sections and compliance docs were updated, but `docs/rapport_snort_rag_devoir3.docx` and `docs/rapport_snort_rag_devoir3.pdf` should be regenerated after the real LLM and Snort/PCAP runs. Otherwise the final binary report may still miss the latest evidence.

Success condition: final DOCX/PDF includes real model names, LLM metrics, Snort PASS/FAIL/SKIPPED counts, PCAP alert counts, limitations, and the strict compliance matrix.

## Current Code-Side Health

Completed checks:

```bash
PYTHONPATH=src pytest tests/
PYTHONPATH=src python scripts/validate_dataset.py
PYTHONPATH=src python scripts/final_health_check.py
```

Current `final_health_check.py` result: no errors, with warnings for mock-only LLM benchmark and SKIPPED Snort/PCAP evidence.
